export type User = {
  id: string;
  email: string;
  displayName: string;
  firstName: string;
  lastName: string;
  refreshToken: string;
  avatar: string;
  signUpDate: Date;
  consumerId: string;
  authorId: string;
}
